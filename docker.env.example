# StreamGank Docker Environment Configuration
# Copy this file to .env and fill in your actual values

# ⚠️  IMPORTANT: ALL VARIABLES MARKED AS **REQUIRED** MUST BE SET!
# ⚠️  The application will fail to start if any required variable is missing.

# =============================================================================
# 🚨 REQUIRED VARIABLES - APPLICATION WILL NOT START WITHOUT THESE
# =============================================================================

# **REQUIRED** - Application environment (local/prod)
# Set to 'local' to use hardcoded HeyGen URLs instead of making API calls
# This saves credits during development
APP_ENV=local

# **REQUIRED** - Data storage path for persistent volumes
# 🖥️  FOR LOCAL DEVELOPMENT (Windows/macOS):
STREAMGANK_DATA_PATH=./docker_volumes
# 
# 🐧 FOR LINUX VM DEPLOYMENT (uncomment and use instead):
# STREAMGANK_DATA_PATH=/var/lib/streamgankvideos

# **REQUIRED** - Docker container settings
NODE_ENV=production
PORT=3000
PYTHONPATH=/app
PYTHONUNBUFFERED=1

# **REQUIRED** - Redis database configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=uJ8IC0FR8da9G05RrsgARpv2BbqoQQIf
REDIS_DB=0         # Redis database number (0-15)
# Database selection:
# 0 = Production
# 1 = Development  
# 2 = Testing
# 3 = Staging

# =============================================================================
# 📋 OPTIONAL VARIABLES - HAVE DEFAULTS OR NOT ALWAYS NEEDED  
# =============================================================================

# The STREAMGANK_DATA_PATH will contain all persistent data:
# - {path}/assets/     (generated assets)
# - {path}/videos/     (output videos)  
# - {path}/outputs/    (processed outputs)
# - {path}/redis/      (Redis data)
# - {path}/logs/       (application logs)

# =============================================================================
# 🔧 SERVICE CONFIGURATION - OPTIONAL
# =============================================================================

# =============================================================================
# SUPABASE DATABASE CONFIGURATION
# =============================================================================
SUPABASE_URL=
SUPABASE_KEY=

# =============================================================================
# AI SERVICE CONFIGURATIONS
# =============================================================================

# OpenAI Configuration
OPENAI_API_KEY=

# HeyGen Configuration
HEYGEN_API_KEY=
# Note: When APP_ENV=local, HeyGen API calls are skipped and hardcoded URLs are used

# =============================================================================
# VIDEO PROCESSING SERVICES
# =============================================================================

# Creatomate Configuration
CREATOMATE_API_KEY=
CREATOMATE_PROJECT_ID=

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Video Generation Settings
DEFAULT_NUM_MOVIES=3
DEFAULT_SCROLL_SPEED=ultra
STRICT_EXACT_DURATION=true

# Logging Settings
LOG_LEVEL=info

# =============================================================================
# DOCKER-SPECIFIC SETTINGS
# =============================================================================

# Node.js GUI Server
NODE_ENV=production
PORT=3000

# Python Backend
PYTHONPATH=/app
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1      # Set to 0 for debugging/development
